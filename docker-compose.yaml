version: '3.1'

services:

  # frotend:
  #   build: ./src/frontend
  #   ports:
  #     - 80:8501
  #   networks:
  #     - postgres-network

  database:
    image: 'postgres:latest'
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=icecreamempire_s4
      - POSTGRES_DB=postgres
      - PGDATA=/var/lib/postgresql/data/pgdata
    networks:
      - postgres-network
    volumes:
      - ./db-data/:/var/lib/postgresql/data
      - ./src/db/init.sql:/docker-entrypoint-initdb.d/init.sql

#   pgadmin:
#     image: 'dpage/pgadmin4:latest'
#     ports:
#       - 15433:80
#     env_file:
#       - .env
#     networks:
#       - postgres-network
#     depends_on:
#       - database
#     volumes:
#       - ./pgadmin-data/:/var/lib/pgadmin

networks: 
  postgres-network:
    driver: bridge